
type vivienda {
    _id: ID
    inner_id: String
    manzana: String
    territorio: String
    direccion: String
    estado: String
    ultVez: String
}

type Query {
    "Devuelve la primera vivienda"
    getVivienda: vivienda

    "Devuelve una vivienda por inner_id"
    getVivienda2(inner_id:String!): vivienda
}

type Mutation {
    
}
